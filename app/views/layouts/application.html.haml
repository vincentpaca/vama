!!!
%html
  %head
    %title VAMA Motors Marketing
    = stylesheet_link_tag "application"
    = javascript_include_tag "application"
    = csrf_meta_tag
  %body
    #container
      #header
        %h1= link_to "VAMA Motors", root_url

        = render 'shared/user_nav'

        = render 'shared/main_nav'

      #wrapper.wat-cf
        .flash
          - flash.each do |type, message|
            %div{:class => "message #{type}"}
              %p= message
        #main
          = yield
          #footer
            .block
              %p
                Copyright © #{Time.now.year} VAMA Motors.
        #sidebar
          = yield :sidebar
